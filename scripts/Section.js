//presentar una lista de elementos en una pagina

export default class Section{
  constructor( { items, renderer } , selectorCardElements ){

  }
  renderer(){ // renderiza todos los elementos en la pagina.

  };
  
  addItem(){ // toma un elemento del DOM y lo agrega al contenedor

  };
}